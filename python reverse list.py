# python reverse list

The reverse list python function allows the programmer to reverse the
sorting order of the elements stored in the lists. Python provides a built-in
function called as the reverse() function which reverses the order of the data elements in place.

